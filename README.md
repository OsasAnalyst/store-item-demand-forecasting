# store-item-demand-forecasting
Forecasting product demand across multiple stores and items using classical (ARIMA/SARIMA), machine learning (XGBoost, LightGBM, Random Forest), and deep learning models. Includes baselines, model evaluation, visualizations, and deployment-ready models.
